# Trained model artifacts are large binaries — keep them out of git.
# They live on disk locally; use Git LFS or external storage if you need to
# share them. The ML service loads them from ./models at startup.
models/

# LayoutLMv3 weights (481 MB) — config/tokenizer files ARE committed, weights are not
layoutlmv3_invoice/model.safetensors

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
